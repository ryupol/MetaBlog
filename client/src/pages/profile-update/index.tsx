import EditCard from "./edit-card";

function ProfileUpdate() {
  return (
    <main
      data-testid="profile-update"
      className="theme-base flex min-h-screen w-full items-center justify-center"
    >
      <EditCard />
    </main>
  );
}

export default ProfileUpdate;
